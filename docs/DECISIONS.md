# DECISIONS: architectural decision log

ADR-lite format: one decision = date, choice, rationale, rejected alternative. A decision is superseded by adding a new entry that references the old one, never by rewriting history. Before reopening a choice during a session: read this first.

| # | Date | Decision | Why | Rejected |
|---|---|---|---|---|
| 1 | 2026-07-18 | A proxy written from scratch, not a wrapper over claude-code-router/LiteLLM | Explicit learning goal plus full control over UX and the doctor | Forking or wrapping CCR |
| 2 | 2026-07-18 | Positioning on verification and curated knowledge (doctor, verified profiles, scoreboard), not on routing | On the "a proxy that translates" axis CCR wins on day one; on the "I tell you what actually works" axis nobody is there | Competing head-on on routing features |
| 3 | 2026-07-18 | Slot mapping inside the proxy, not through `ANTHROPIC_MODEL`/`ANTHROPIC_DEFAULT_*` | Hot profile switching without restarting Claude Code; a coherent Claude Code UI | Mapping through env vars |
| 4 | 2026-07-18 | `lupin run` exports the env to the whole process tree | It covers hooks and plugins that make their own SDK calls (claude-mem); without it the coverage is partial in an invisible way | Asking the user to set the env by hand |
| 5 | 2026-07-18 | Passthrough always preferred when the provider is Anthropic-native | Zero translation risk; translate is the fallback, not the default | Uniform translation for everyone |
| 6 | 2026-07-18 | A doctor with no hidden retries (bare score; the double bare/adapted score arrives in M5) | The doctor must predict the real experience; hidden retries would falsify it | A "kind" doctor that hides the flaws |
| 7 | 2026-07-18 | Quirks and dialect normalizations: centralized per-profile flags, never on by default for cloud providers | Avoid `if (provider === x)` scattered around; do not mask regressions of serious providers | Normalization always on |
| 8 | 2026-07-18 | Stack: TypeScript strict + Node >= 20, hono/fastify, vitest; distribution through `npx` | The same ecosystem as Claude Code, zero install, no mandatory native dependencies | Python (more packaging friction for this audience), Go/Rust (slower iteration for a learning project) |
| 9 | 2026-07-18 | Gemini through the OpenAI-compat endpoint in v1; a native adapter only when the limits bite | One adapter fewer to write and test right away | A native Gemini adapter in v1 |
| 10 | 2026-07-18 | Fixtures recorded from real provider output, not written from memory | Real dialects are stranger than you would guess; an invented fixture tests the imagination, not the provider | Synthetic fixtures |
| 11 | 2026-07-18 | Never write into `~/.claude/settings.json`; the user config is untouchable | Removing Lupin means not using `lupin run`; zero side effects on the environment | Auto-configuring settings.json |
| 12 | 2026-07-18 | Zero persistence of prompts and responses (unless `LUPIN_DEBUG` is explicitly set) | The proxy sees all the user's code: trust is a product requirement | Logging bodies for convenient debugging |
| 13 | 2026-07-18 | Apache 2.0 license | Permissive plus an explicit patent grant; the standard for dev tooling that wants adoption | MIT (no patent grant), AGPL (slows adoption) |
| 14 | 2026-07-18 | HTTP framework: hono + @hono/node-server | Web-standard Request/Response, so the provider stream can be piped directly (byte-per-byte passthrough), lightweight, TS-first | fastify (Node-specific stream API, heavier for a pipe-first proxy) |
| 15 | 2026-07-18 | npm package name `lupin-code` plus the scoped mirror `@fanfulla/lupin`; the bin command stays `lupin` | `lupin` on npm is taken by an abandoned package (v0.0.0, 2022); an npm dispute is slow and uncertain | An npm dispute for `lupin`; publishing scoped only |
| 16 | 2026-07-18 | OpenRouter: automatic per-model routing. Passthrough (the Anthropic skin) for Anthropic models, translate (M2) for everything else. NO user-facing limitation: coverage stays universal, only the internal lane changes, and Lupin picks it | Official OpenRouter docs (verified 2026-07-18): the Anthropic skin "is only guaranteed with the first-party Anthropic provider", a provider constraint, not a Lupin one. The universal translator (M2) covers the rest | Universal passthrough through the Anthropic skin (not supported by the provider); exposing the lane choice to the user |
| 17 | 2026-07-18 | Two orthogonal, decoupled axes: **protocol** (passthrough/translate = the translation core, M2) and **credential source** (native/OpenRouter API keys vs subscription tokens = the auth layer, M3 onwards). A subscription is a pluggable credential source, opt-in per provider, verified by the doctor. It applies ONLY to non-Claude providers (for Claude models inside Claude Code one uses the Claude plan, OpenRouter or a native key, see ADR-16 and the 2026-07-18 session). The API key stays the guaranteed "floor" the doctor promise rests on | The strong pitch is "use GPT/Gemini/Kimi with the subscription you already pay for", but consumer subscriptions do not include the API, so it needs the same OAuth replay the community proxies use (provider ToS, ban risk, fragile): it must be isolated from the core and must not carry the guarantee. Decoupling the two axes keeps the translation core intact | Coupling subscriptions and the translation core; resting the guarantee on the fragile path; a subscription bridge for Claude/Anthropic (excluded during the session) |
| 18 | 2026-07-18 | Anthropic as a target provider and the Claude models: OUT OF SCOPE. Lupin serves third-party providers (Kimi, DeepSeek, GLM, OpenAI, Gemini, local runtimes and so on) | Anthropic is already the native backend of Claude Code: integrating it adds nothing. All the effort goes into integrating the others as deeply as possible | An M1 null test against Anthropic as a mandatory criterion (downgraded to optional); any work dedicated to Claude models through Lupin |
| 19 | 2026-07-18 | Pluggable credential source implemented (DESIGN-OAUTH): an `AuthConfig` union (`bearer`/`x-api-key`/`oauth`), a unified resolver in `server/credential.ts`, and the first OAuth flow being Kimi Code through the Device Authorization Grant (RFC 8628), with no callback in the server. Proactive refresh (the kimi-cli half-life rule, 5 minute floor), single-flight, reactive retry-once on 401, tombstone on `invalid_grant`. Store: `~/.lupin/credentials.json` at 600 with tokens under `oauth/<provider>` keys | The device flow is the real one used by the official Kimi CLIs (public client_id, auth.kimi.com endpoints verified 2026-07-18); zero new HTTP surface; the core does not know where the token came from (ADR-17) | Authorization Code + PKCE with a localhost callback (disproved by the research: Kimi uses the device grant); tokens in the config file; a retry loop on auth |
| 20 | 2026-07-19 | Long-lived auxiliary processes already running (for example the claude-mem worker) are OUT of `lupin run` scope: never killed, restarted or reconfigured. Lupin covers only what starts inside its own process tree (this refines ADR-4). Criterion M1-2 is redefined accordingly: in-session hooks and plugins go through the proxy (already verified), while the worker's own SDK calls are excluded and follow the credential the worker was born with. Documented caveat: a worker started for the first time INSIDE a `lupin run` inherits the proxy env and keeps it after the session too | Verification cannot ask the user to stop processes by hand: the flow must stay smooth and ordinary (user feedback 2026-07-19). Touching someone else's processes also violates zero side effects (ADR-11) and on Windows risks zombie ports | Killing or restarting the worker as a verification step; Lupin managing the worker env; criterion M1-2 as originally written |
| 21 | 2026-07-19 | Local runtimes passthrough-first: Ollama (>=0.14) and LM Studio (>=0.4.1) through the native Anthropic Messages API on `/v1/messages`; translate only as a fallback and for llama.cpp. Model discovery always through the OpenAI-compat surface `GET <translateBaseUrl>/models` (same port) | Live verification 2026-07-19 (Ollama 0.21.0 answers Anthropic-native with thinking/usage; the LM Studio endpoint is active) plus official docs. Passthrough removes the translation risk, the most reported bug class in competitors (CCR: corrupted reasoning, tool calls and usage); the local runtime normalizes the model's own dialects. A missing `count_tokens` (404) is covered by the local estimate already planned (SPEC-PROVIDERS §8) | Translate-only local runtimes (the first draft of §3ter, superseded by verifying the native endpoints); a dedicated quirk for count_tokens |
| 22 | 2026-07-19 | Dialect normalization (§5bis) implemented with ONE stateful engine (`core/dialect.ts`) shared by the non-streaming mapper and the SSE translator, with hold-back of partial markers and a "streaming == non-streaming" test invariant. `reasoning_content` mapped to a `thinking` block (no longer dropped). Tool calls recovered from text rewrite `stop_reason` to `tool_use`. An orphan closing tag is only stripped, never reclassified | The largest bug class in competitors (CCR: #1397 corrupted tool-call deltas, #1356 broken content blocks, #1041/#1400 lost reasoning) comes from normalizations diverging between the two paths and from dropped reasoning. A single engine makes divergence impossible by construction. The unlimited lookahead an orphan tag would need costs time-to-first-token, which is unacceptable for a proxy, and the real recovery goes through reasoning_content anyway | Two implementations (one per path); buffering the whole message to reclassify the orphan tag; an external dependency for JSON repair |
| 23 | 2026-07-19 | The doctor NEVER produces a score from a session that did not reach the model. Two guards: (a) a **preflight** on the context window declared by the profile, threshold `DOCTOR_MIN_CONTEXT` = 50000 (46075 tokens measured live for the doctor task plus output headroom), checked on the slot with the lowest value; (b) an **honest reading** of the headless result, using `is_error` and `terminal_reason`, not `subtype` alone. A transport `terminal_reason` (`api_error`, `auth_error`) replaces the score with the `notRun` field, exits 1, and `lastDoctor` is NOT written. A model too weak to finish instead keeps its score | The first real run on LM Studio (gemma-4-12b loaded at 8192): every request failed on context overflow, and the doctor printed 1/10 with check 1 at "session completed", because Claude Code answers `subtype:"success"` even when its loop ends on `{"is_error":true,"terminal_reason":"api_error"}`. The number accused the model of an infrastructure limit the profile already knew about (`contextWindows`). The M4 criterion asks for a low verdict that is **explained**: a wrong score is worse than a crash, because it is credible | Score 0 with a side note (it still reads as a judgement on the model and lands in the history); trusting `subtype`; measuring the window with a probe request instead of from the profile |
| 24 | 2026-07-19 | Errors inside an SSE stream with HTTP status 200: the passthrough body stays untouched byte per byte (ADR-7), but a **tap that observes without modifying** emits a second log line with `streamError`. Same scheme already used for `dialect` | LM Studio answered an over-context request with `HTTP 200` plus `event: error`: `lupin logs` showed a clean 200 for a request that had failed entirely, making diagnosis impossible from the only tool the user has. Modifying the stream to "fix" the status would break passthrough fidelity, which is the reason passthrough exists | Rewriting the HTTP status downstream (breaks ADR-7); buffering the stream to inspect it (kills time-to-first-token); leaving the log silent |
| 25 | 2026-07-20 | (a) Failover with a **per-profile cooldown** (SPEC-PROVIDERS §4sexies): an in-memory counter over retryable failures; at 3 consecutive ones the profile pauses for 60s and requests skip the dead one and go straight to the failover; visible in the log and in `lupin list`, never persisted. (b) Routes and failover stay **always opt-in**: Lupin NEVER activates routing silently, but `lupin init` **offers** what discovery and the registry can infer (a vision route from capabilities, a tier-equivalent failover) and the user accepts or not (default "no") | (a) Observed live: a profile with no failover took three consecutive 529s (saturated upstream) that arrived raw as "at capacity"; the cooldown sends those turns straight to the alternative instead of retrying the dead one on every request. It is the minimal version of the Bifrost/LiteLLM circuit breaker, without their enterprise state machine. (b) The routing research pushed for "config only to turn things off", but ADR-7 forbids normalizations that are on by default, and the user confirmed "never auto-activate". An explicit offer at init solves discoverability without violating the principle: the decision stays human | A cooldown persisted to a file (it is short-term health, and a restart is already recovery); auto-activating routes from discovery (would violate ADR-7); a 4-state Bifrost-style machine (out of scope for a single user); complexity routing and subagent routing (deferred to M5, scope decision 2026-07-20) |
| 26 | 2026-07-22 | OS keychain by default through `@napi-rs/keyring` in `optionalDependencies` (keytar has been archived since 2023): the selection is cached per process. An explicit path or `LUPIN_CREDENTIALS` means file; `LUPIN_CREDSTORE=file` means file; a successful write/read/delete probe means keychain; otherwise the 600 file, and the choice is visible in `lupin status`. Base64 chunking above 2048 bytes (the Windows blob cap is around 2560), and values identical to a chunk header take the chunked path anyway, so a read can never misinterpret them (commit 67d159f). Lazy file-to-keychain promotion with a read-back check before deleting the file copy. Never a mid-process downgrade to file after a successful probe | SPEC-CLI §4 prescribes a keychain first with a file fallback; there was no installed base to migrate (user decision 2026-07-22); a module that loads is not a working backend (Linux without a Secret Service imports fine and then throws); a tombstone that leaves a file copy behind is not a tombstone | Shelling out to the OS tools (secrets in a subprocess argv or stdin: a security regression); a three-layer cross-keychain architecture (the middle layer covers an almost empty set); assisted migration; a silent per-operation downgrade (split-brain writes) |
| 27 | 2026-07-28 | The TUI hub is an OPTIONAL Rust ratatui sidecar (`tui/`, binary `lupin-tui`), talking to the daemon over a loopback control API (`/v1/lupin/*`). The core proxy stays pure Node with zero native dependencies (ADR-8); `lupin` (no args) launches the sidecar only when it is on the PATH and stdout is a TTY, and falls back to a text status everywhere else | User decision 2026-07-28 (ratatui over a Node zero-dep TUI). A sidecar keeps the native code out of the request path and out of the npm package: a user without a Rust toolchain loses nothing. The control API (127.0.0.1, localToken-guarded, metadata only) is the one write path, so the config file stays the single writer and the hot-reload watch the single reload trigger; OAuth logins and doctor runs are async jobs because both are long-running and the TUI must stay responsive | A Node zero-dep TUI (the user's second choice); embedding the TUI in the daemon; a second IPC mechanism (named pipes/sockets) instead of reusing loopback HTTP and the existing localToken |
| 28 | 2026-07-28 | OAuth gains a second flow kind, Authorization Code + PKCE (loopback redirect), alongside the Kimi device flow (ADR-19). OpenAI is implemented (sanctioned Sign-in-with-ChatGPT, verified 2026-07-28: client `app_EMoamEEZ73f0CkXaXp7hrann`, redirect localhost:1455, scope `openid profile email offline_access`). Gemini is implemented but gated behind an explicit `--i-accept-the-risk` because Google suspends accounts for third-party OAuth (mass bans Feb-Mar 2026). Z.AI stays API key (no public OAuth exists). The Gemini client_id and (public) client_secret were captured from the official gemini-cli source on 2026-07-28 (`packages/core/src/code_assist/oauth2.ts`): an installed-app client on an ephemeral-port loopback at `/oauth2callback`, scopes `cloud-platform`/`userinfo.email`/`userinfo.profile`; Google demands the secret even with PKCE, so the descriptor carries an optional `clientSecret` | The research (2026-07-28) shows OpenAI is the only major provider still sanctioning third-party OAuth to a subscription; Google's ban makes its OAuth a real account risk the user chose to expose behind a warning, never as a default. Both PKCE providers converge on the same OAuthTokens record and the same refresh runtime as the device flow, so the core still does not know where the token came from (ADR-17). The flow is re-derived from the public-client shape (codex-rs/login, Apache-2.0) rather than copied | Dropping Gemini OAuth entirely (the user accepted the risk and wanted the choice); inventing a Gemini client_id (captured from the official source instead); a single OAuth flow forced onto every provider |
| 29 | 2026-07-29 | `lupin run` spawns the child WITHOUT a shell whenever one is not forced, on every OS (`resolveHead` in `src/cli/run.ts`). On Windows the head is resolved the way cmd resolves it: the current directory first (unless `NoDefaultCurrentDirectoryInExePath` opts out, exactly like cmd), then PATH, each with the PATHEXT extensions in order; an .exe/.com spawns directly with full argv fidelity; a .cmd/.bat keeps cmd.exe (batch files cannot be spawned without a shell, EINVAL since Node 20.12.2), but then the announcement settings travel as a FILE under LUPIN_DIR (directory created on demand; any write failure costs the announcement, never the session), never inline JSON, because a quoted path is the only payload cmd.exe passes byte for byte | The startup announcement (same day) was the first argument lupin ever injected with quotes and spaces, and it surfaced a latent bug: `shell: true` makes node join the args with no quoting, so cmd.exe mangled the inline `--settings` JSON and claude refused to start ("Invalid JSON provided to --settings", reproduced live against claude.exe 2.1.220). Hand-rolled cmd escaping (cross-spawn style, single and double) was tried and fails through a `.cmd -> %* -> node` chain: three parsers (cmd /c, batch %*, msvcrt) eat each other's backslashes. Regression tests in `test/run-spawn.test.ts` pin both spawn shapes, Windows shim included. Left on purpose: through a .cmd shim the user's OWN args with quotes/spaces still pass raw (status quo; cmd.exe cannot carry them reliably); the same latent `shell: true` pattern in `doctor/run.ts` was moved onto `resolveHead` the same day (the audit reproduced its mcp-config-path truncation empirically); `cli/hub.ts` and `cli/skills-offer.ts` spawn fixed args and stay as they are | Escaping the JSON for cmd.exe by hand (empirically lossy); skipping the announcement on .cmd installs (silent feature loss); vendoring or depending on cross-spawn for one spawn site |
| 30 | 2026-07-29 | A 24-agent adversarial audit of every CLI command and the TUI (15 findings confirmed, 1 refuted) turned into a hardening sweep with one invariant: an auxiliary feature degrades to silence, never to a broken session or terminal. Fixed: `top` guards `setRawMode` behind `stdin.isTTY`, restores the terminal in a `finally`, and validates the /health shape before walking it; the hub's sidecar probe gets a 3s timeout; `lupin go` checks its usage BEFORE switching the profile, so a forgotten `--` no longer changes state; `logs -f` slices bytes instead of a decoded string, so multibyte log content cannot drift the tail; `init`'s hidden-input backspace removes a code point, never half a surrogate pair | Every fix is a confirmed finding with a concrete failing scenario, verified adversarially before touching code. Deliberately NOT changed: the TUI's blocking fetches (800/1500ms timeouts bound the freeze), ratatui's Unicode borders (legacy conhost raster fonts garble them; every modern terminal is fine), `top` has no terminal-size awareness, `lupin stop` leaves the watchdog holding the port for up to five minutes (that is SPEC-CLI §6.4 doing its job: the 529 text tells the session how to restart), and `run-spawn` tests pin the extracted helpers rather than `runCommand`'s full wiring | Fixing everything the audit surfaced in one sweep (the leftovers are design questions or accepted tradeoffs, not oversights, and each is named here so nobody rediscovers them) |
| 31 | 2026-07-29 | `lupin-tui` v2 is a dashboard: bordered panels (header with plain-ASCII gentleman-thief art, profiles with the 1-9 hotkey fused INTO the row label so no width squeeze can hide it, serving-now, a per-window stats panel, the request tail) plus a talking status line that narrates every action and its outcome; `r` refreshes on demand; `switch_profile` returns a Result so a failed switch comes back as words instead of a silently unchanged screen; the FREE tier is announced only when /health declares it (the M6b honesty rule). CLI parity is structural, not duplicated logic: the screen reads the same config file and /health the CLI reads and writes through the same control API `lupin use` calls, so the two surfaces cannot disagree for longer than one refresh | User request 2026-07-29: a minimal, functional, talking dashboard with Lupin ASCII art. The art is ASCII-only on purpose (legacy conhost raster fonts); the borders stay Unicode (accepted in ADR-30). The TestBackend suite grew from 15 to 19 tests and the screen was driven live in tmux on Linux and on a real Windows console the same day | A tabbed multi-screen TUI (the dashboard answers at a glance); box-drawing art (garbles on legacy conhost); duplicating `lupin usage` aggregation in the TUI (a pretend "today" computed from a 64KB tail would lie: the stats panel counts only the window it shows, and the full aggregation stays in `lupin usage`) |
| 32 | 2026-07-31 | The scenario-B handoff (backlog #16): a native Anthropic session continues on a third-party profile via `lupin resume [<profile>]`, a thin wrapper over `use` + `run` spawning `claude --continue`. The transcript replay is the continuity mechanism: lossless, client-owned, zero new protocol. Probe before code (`scripts/handoff-replay-probe.mts` must record live whether a passthrough target accepts Anthropic-signed thinking blocks in replayed history); a `stripHistoryThinking` opt-in quirk exists only if the probe proves a 400. ADR-18 boundary read and kept: the handoff targets the third-party profile, never routes to Anthropic, and only stats transcript file sizes (never content, never a write into `~/.claude`). Full design and dated evidence in `docs/DESIGN-HANDOFF.md` | The mechanics already work today (`lupin go <p> -- claude --continue` forwards args intact, verified src/cli/run.ts 208-227); what is missing is one gesture, the cwd rule said out loud, and the honest cold-prefix advisory. All public prior art (9router, CLIProxyAPI, opencode, UncommonRoute) documents signature 400s only in the opposite direction (foreign history INTO Anthropic), so our direction needs its own live probe, not an assumption | Context compression in the proxy (rewriting history breaks byte fidelity and signatures, parsing content breaks the privacy rule; the client owns `/compact` and headroom-class tools stack client-side unchanged); goodboy-style summarize-and-rebuild continuity (lossy by construction when the client already holds the full transcript); building the quirk before the probe answers |
| 33 | 2026-07-31 | Quota-aware durable switch (backlog #16b, SPEC-PROVIDERS §4octies): a quota-exhausted answer on the active profile (matched by a central per-provider registry with verification dates; today only kimicode's "usage limit ... billing cycle" 429, seen live 2026-07-29) moves the active profile to its declared `failover` ONCE, through the same config write as `lupin use`. Failed requests now log the provider's error message (scrubbed, 200 chars) so the next quota shape records itself. No automatic switch-back: the reset time is unknowable (Kimi renewed unannounced between 07-29 and 07-31) | The status alone cannot separate "retry in a second" from "dead until next month": only the message can, and the log could not even show the message. The switch keys on the exhausted answer, not the failover attempt's outcome, because the primary stays spent either way. Opt-in rides the existing `failover` field: it already diverts traffic per-request, so the durable move only stops re-paying a doomed request per turn (user decision 2026-07-31, log-message consent same day) | A separate opt-in flag (more config for the rare case); auto switch-back on a guessed reset time (a guess that lies); treating Code Assist RESOURCE_EXHAUSTED as durable (the free tier emits it per-minute: it would flap); classifying by status alone |
| 34 | 2026-07-31 | The user's automatic-switch order IS the failover chain (user request 2026-07-31: a per-user order editable from the TUI). No new routing concept: `kimi-sub -> openai-sub -> gemini-sub` is three `failover` fields, §4ter serves the per-request hop and §4octies walks the chain over time as cycles exhaust. One atomic control endpoint, `POST /v1/lupin/switch-order { order: [...] }` (2+ existing names, no duplicates), sets each element's failover to the next and DELETES the last one's: a chain has an end, not a loop. The TUI edits it: `o` enters order mode, the typed profile numbers build the chain previewed by name in the talking status line, Enter applies through the endpoint, Esc cancels. Rows show their link as `-> name` | The order the user wants already had a faithful representation in config; an editor beats a second concept. Atomic beats per-link writes: no partial chain can survive a mid-edit failure. The last link is cleared so a reorder never leaves a stale tail | A separate `switchOrder` array in the config (two sources of truth for one behaviour); per-link endpoints (partial-write states); letting the order form a cycle (a durable ping-pong when everything is exhausted) |
| 35 | 2026-08-02 | ds4 re-verified on source (HEAD 54b36ed, three-agent pass; `ds4_server.c` untouched since the 2026-07-24 verification) and the findings recorded as SPEC-PROVIDERS §3ter facts, NOT translate quirks: passthrough forwards bodies untouched, so a provider-side gap (no count_tokens, images dropped silently, forced tool_choice ignored, mid-decode failure ending as a clean `end_turn` 200) is documented truth, not mappable behavior. The one actionable piece becomes the first launch-env quirk, `raiseStreamIdleTimeout` (on by default for the ds4 profile): `lupin run` fills `CLAUDE_STREAM_IDLE_TIMEOUT_MS=600000` when unset, implemented ONLY in `runEnv` reading the launch-time profile's quirks | ds4-server serializes requests on one slot and keeps one live KV checkpoint: a queued request gets zero bytes until its turn, and upstream's own Claude Code wrapper raises the client idle timeout to 600000 ms for exactly this. Filling an unset env var mirrors the existing `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` precedent: the user's explicit value always wins. The ROADMAP #13 open question is answered from source: usage DOES report `cache_read/creation_input_tokens` from the server's own KV accounting, so the doctor's cache receipt will work when hardware exists | Setting `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC` too (changes client behavior beyond keeping the stream alive: documented instead); a proxy-side keep-alive injector (the passthrough tap must never write into the stream, ADR-24); waiting for live hardware before recording source facts (the repo is beta and fast-moving: dated source truth beats nothing) |
| 36 | 2026-08-02 | Several accounts on one provider (backlog #15b, SPEC-PROVIDERS §4nonies) are **a credential-store key and therefore a profile**, not a new runtime concept: `oauth/<provider>` gains an optional `#<account>` suffix, `lupin login/logout <provider> --account <label>` writes and forgets it, the default subscription profile becomes `<provider>-sub@<label>`, and `auth.provider` carries the full key. The descriptor is looked up on the provider half of the key, so both accounts share one flow and one refresh runtime, while the single-flight lock and the `invalid_grant` tombstone key off the STORE key so the two refresh independently. **Rotation is the existing chain**, `failover` per request (§4ter) plus the quota-aware durable switch over time (§4octies), in the order the user sets from the TUI (ADR-34) | The whole difference between two accounts is which token pays; the descriptor, flow, lane and refresh belong to the provider, so anything more than a key suffix would duplicate them. A profile is already "one credential plus its slots", which is exactly what an account is, and it makes the account visible in every surface that names a profile (`lupin list`, `/health`, statusline, TUI, log) with no new field. A bare key with no suffix stays byte-identical, so no existing login migrates | A dedicated round-robin rotator (ADR-34 rejected a cyclic chain for a reason: when every account is spent a loop ping-pongs forever instead of surfacing the exhaustion); an `accounts[]` array inside one profile (it would need per-account slots, health, cooldown and doctor state, which is a profile); sanitizing a bad label instead of refusing it (a `#` or `/` inside a label makes the store key ambiguous) |
| 37 | 2026-08-02 | Switching profile from INSIDE Claude Code (backlog #16a, SPEC-PROVIDERS §4.3): `GET /v1/models` publishes one inert row per profile, `claude-lupin-switch:<profile>`, appended after the model rows and including the ACTIVE profile; picking one is read by the ingress as the gesture `lupin use <profile>`, acted on only when the target CHANGES, and the carrying request is then served through the new profile's opus slot. An unknown profile serves the session on the active profile and logs `profileSwitch: "unknown:<name>"` instead of failing | The picker is the one client surface Lupin controls, and the research of the day pinned the two facts that shape the design: the client fetches the list ONCE per session and caches it (so the active profile must be listed, or the switch is a one-way trip), and it re-sends the picked id on every later turn (so reading it as a pin would let a stale client echo undo a `lupin use` made from the CLI or the TUI, breaking ADR-27's single-writer rule). A pick confirmed with Enter is saved in the user's GLOBAL settings.json by the client, so an id can come back from another session or another config: serving beats erroring | Reading the id as a pin (the client would fight the config); erroring on an unknown profile (a globally-saved pick would make a whole session unusable until the user re-picked); publishing only non-active profiles (unreachable return trip, given the frozen list); a config flag to hide the rows (configurability nobody asked for: the rows are inert until picked) |
| 38 | 2026-08-02 | GitHub Copilot implemented as an OAuth **device-flow** provider in the translate lane (backlog #15a, SPEC-PROVIDERS §3quater), gated behind `--i-accept-the-risk`, with a second token bought at exchange time (`copilot_internal/v2/token`) whose answer also names the API host, a `nonExpiringToken` descriptor flag so the refresh runtime leaves a non-expiring GitHub token alone, `ProviderDef.requiredHeaders` for the editor headers Copilot demands, and slots discovered from the account's own `/models` at login instead of written into the defaults. The HMAC signing secrets found next to the client id are deliberately NOT used | Two roadmap assumptions were wrong and the research corrected both: Copilot uses RFC 8628 (GitHub's own Copilot CLI docs plus the shared client bundle every editor plugin vendors), not the PKCE flow #15a assumed; and GitHub Models, the sanctioned PAT alternative, was fully retired on 2026-07-30, so there is nothing cleaner to prefer. The exchanged token names a per-account host, so assuming the well-known one would silently misroute some accounts. The editor headers are mandatory (a missing `Editor-Version` answers 400, the most reported bug of every Copilot proxy), which is why they are a field distinct from cosmetic `attribution`. The risk gate is justified by the enforcement pattern, suspensions reported as permanent and tied to the user's main GitHub identity, NOT by a term forbidding third-party clients: none was found, and the copy says so rather than overclaiming | Passthrough through the Anthropic-shaped `/v1/messages` the backend also exposes (real, but nobody has shown it answering a plain exchanged token, and the one mature third-party implementation routes even Claude models through `/chat/completions`); reusing the bundle's HMAC secrets (they are gated to internal build channels: that is forging an authenticity signature, not reusing a public client); writing any model id into the defaults (per account, fast moving, and not one verified: the account's own `/models` is the source); shipping without the risk gate; combining this provider with §4nonies multi-account, which is the exact pattern those suspensions are associated with |
| 39 | 2026-08-02 | `identityHint` (backlog #9, SPEC-PROVIDERS §5ter): an opt-in per-profile quirk appending ONE system block that names the model and provider really answering, built at runtime from the resolved target, appended LAST, and never applied to `count_tokens` | Through a proxy the model reads the harness prompt and introduces itself as Claude, so asking it who it is proves nothing; the statusline answers this for the eye but not for the conversation. Appending is what keeps the answer cheap: every earlier byte is unchanged, so the provider's cached prefix and its `cache_control` breakpoints survive, while prepending would re-prefill the whole harness every turn. Off by default because it edits the request body (ADR-7) | Prepending the hint (cache death); injecting it into the messages array (it is not a turn, and it would drift into the transcript the client owns); rewriting the client's own system text (destroys the prefix and lies about what the harness said); claiming it beats the harness prompt, which is untested until a live session says so |
| 40 | 2026-08-02 | The cache-bust detector (backlog #11c, `src/server/cache-watch.ts`) is built from `usage` counts, NOT from prompt prefixes: per profile it keeps the previous request's `cache_read` and total input, and logs `cacheBust: true` when the cache goes warm to cold while the prompt did not get smaller. A provider that reports no cache fields is never judged, and its history is dropped | The backlog proposed holding prefixes in memory and diffing them; two integers answer the same question with no prompt bytes held at all, not even hashed, so ADR-12 is not stretched and a heap dump contains nothing that was not already a token count. The size comparison is what keeps it from crying wolf: a new conversation also starts cold, but it starts SMALL, while a busted prefix re-sends everything and gets nothing back. It needs no invented threshold, since the comparison is against what that same profile really sent last time | Keeping prefixes (or their hashes) in memory (content retention for a diagnostic, against the privacy rule that is a product requirement here); a fixed token threshold (a magic number that would be wrong per provider and per session); failing or retrying the request (nothing failed: it was slower and dearer, and saying so is the whole point) |
| 41 | 2026-08-05 | The repository goes public **with its whole documentation set**, which reverses the pre-publication note of 2026-07-19 ("remove the internal .md files from GitHub with `git rm --cached` plus `.gitignore`"). Specs, the ADR log, the roadmap, the audit, the competitive and market notes and the superpowers execution artifacts all ship. Published at 0.1.0 on the npm `latest` tag rather than a release candidate on `next`, so the `npx lupin-code@latest` the README teaches actually resolves. Three files a public repo is judged by were written first: SECURITY.md (which properties are worth breaking, and that a proof of concept must never carry a real credential), CODE_OF_CONDUCT.md routed to GitHub private reporting so no personal address is published, and CHANGELOG.md | The July note was written when the docs were private working notes. They are now the strongest evidence the project has: every number in the README is traceable to a dated measurement in `docs/`, and a reader who wants to check the "10/10" can follow it to the session that produced it. Hiding that would leave the README making claims with nothing behind them, which is exactly the posture COMPETITIVE §D names as the top user complaint about routers. The `latest` tag follows the same logic: a quickstart that fails is worse than a prerelease label | Stripping the internal docs (the README's thirteen doc links would break, and the credibility argument above is lost); publishing spec and ADR but hiding audit and market notes (a half-open record invites the question of what is missing); `0.1.0-rc.1` on `next` (the README's own `npx lupin-code@latest` would fail for every new reader); publishing npm before the repo is public (badges and every doc link would 404) |
| 42 | 2026-08-05 | `lupin use` gains `--opus/--sonnet/--haiku` and, more importantly, **refuses any flag it does not recognise** (SPEC-CLI §1). `lupin login` prints the discovered account catalogue, four names to a line, with the aiming command and the caveat that being listed does not mean the plan can use the model; embedding models are excluded from the slots they could never serve | Found live on 2026-08-05 by the first real Copilot login: `lupin login` had been printing `lupin use <profile> --opus <model>` as the way to aim a discovered profile, and that command did not exist. `use` ignored it in silence, so it switched the profile, left the slot untouched, and reported success. That is the tool lying about itself, which COMPETITIVE §D records as the number one user pain point, and it was reachable in the first two minutes of using the newest provider. The catalogue print closes the other half: three places claimed the list was printed and only the count ever was | Validating the model names against the provider (nothing local knows which ids a plan accepts, and an invented validation is worse than none: the command says the names are unchecked); filtering the catalogue by `supported_endpoints` (live, every model declaring `/chat/completions` answered 400 while the five that worked declared nothing, so that field would have filtered out exactly the usable ones); probing every listed model at login to find the usable ones (51 requests on an account whose free plan allows about 50 a month) |
| 43 | 2026-08-06 | When a secret moves from the 600 file into the OS keychain (lazy promotion, or a keychain-mode overwrite of a stale file copy), the file copy is replaced by a **non-secret marker** `{"__inKeychain": true, "movedAt": <iso>}` instead of being deleted. A file-only install that misses a credential consults the marker and says the credential lives in the OS keychain this install cannot read (fix: `npm i @napi-rs/keyring`, or log in again) instead of `no OAuth credentials: run lupin login` about a credential that is intact. The tombstone delete removes the marker too, and a pure-keychain write still never creates the file. Every existing reader treats the marker as absence by construction: `loadCredentials` keeps string values only and `parseOAuth` requires `accessToken`, so only the two miss-path error messages consult it | The split-brain was seen live on 2026-08-05 (recorded as a known limitation in CHANGELOG 0.1.1): a global install promoted a Kimi token that this checkout, without the optional native module, could no longer see, and the advice it gave (re-login) was wrong. The 0.1.1 entry itself said the honest fix was a marker; the user decided to implement it on 2026-08-06 | Deleting the file copy with no trace (the 0.1.1 behaviour: the wrong advice survives); writing the marker on EVERY keychain set including fresh logins (rejected: it would create `credentials.json` on machines that never had a file era, breaking the "file not created when there is nothing to promote" rule in the 2026-07-22 design); a cross-install IPC or lockfile protocol (heavyweight for a hint) |
| 44 | 2026-08-06 | A stream that ends without a terminal event is a **failure on both lanes** (SPEC-TRANSLATION §5 punto 5 and §9.1, issue #1). Translate: `finish()` emits an `event: error` instead of synthesizing `message_delta` + `message_stop`, in all three translators (a `finish_reason` already seen still closes cleanly, since usage arrives after it and `[DONE]` is only the usual place to close). Passthrough: the tap tracks `message_stop`, or a `message_delta` carrying a `stop_reason`, and when neither arrives it logs `streamError: "truncated"` and withholds the `recordSuccess` that clears the §4sexies cooldown. The bytes stay untouched (ADR-7): passthrough observes, it never writes a `stop_reason` the provider did not send | Reported by u/mergethevibes on r/LLMDevs the day of the launch post, and true as written: a 200 that starts streaming and then drops reached Claude Code as a truncated but well formed turn, `stop_reason: end_turn`, because `mapFinishReason(null)` returns `end_turn` and the tap only fired on an explicit `event: error`. It is the same class of lie as ADR-24 (mid-stream failures invisible to health) and the same class as the `subtype: "success"` trap the doctor already refuses: a clean-looking envelope around an answer that never finished. A rescued tool call is the worst payload to deliver from a cut stream, so the dialect flush no longer runs on a truncated close | Reconciling token counts against the streamed content (the counts are the provider's own claim: absent usage already tells the same story, and comparing them would need a tokenizer per provider); recording a `recordFailure` on truncation (unproven attribution: a client-side abort ends the body the same way, and withholding the success is enough to keep a dying provider walking toward cooldown); rewriting the passthrough stream to append a synthetic terminal event (ADR-7, and it would fabricate a `stop_reason` nobody sent); leaving `finish()` closing cleanly and only logging (the log is for the operator, the error is for the client, and the client is the one acting on half an answer) |
| 45 | 2026-08-07 | `editRetryHint` (SPEC-PROVIDERS §5quater), the FIRST M5 behavioural adapter, landed early by explicit user decision (deviation recorded in ROADMAP M5): an opt-in per-profile quirk that appends ONE system block naming the exact-match rule, on the turn whose incoming `tool_result` carries `is_error: true` for a `tool_use` that had an `old_string` (MultiEdit's `edits[]` included). Appended after the §5ter identity block, never on `count_tokens`, implemented at the single ingress seam so both lanes get it | A launch-thread commenter asked the right question: models that are not Claude return edits that are right in meaning and wrong in bytes (re-indented, tabs for spaces, trailing newline dropped), and reporting the failure is not solving it. The expensive part is not the refusal, it is the model resending the same `old_string` for three turns, so telling it the rule once on the turn where it can still act is where the cheap win is. The constant block goes first and the volatile one last, or the identity block's index would move the first time an edit fails and the cached prefix would end at a different boundary (§3ter). Off by default because it edits the request body (ADR-7). Its efficacy is UNMEASURED until the M5 A/B runs, and is documented as such, the same honesty ADR-39 used for `identityHint` | Repairing `old_string` proxy-side (the proxy has neither the file nor a way to know which occurrence was meant, a wrong fuzzy match corrupts a source file, the bytes at proxy time are not the bytes the tool will see, and it would launder the defect the doctor exists to measure); reconstructing the file from the previous `Read` tool_result, which the proxy does see in-band (lossy: line-numbered and truncated at 2000 lines, and still a silent semantic rewrite); firing on any failed tool call (a `Bash` exit 1 has nothing to do with this hint); keeping the hint for the rest of the session after one failure (nags about something already fixed, and is paid for every turn); prepending it (cache death, §3ter) |
| 46 | 2026-08-07 | ADR-45's own verification, run the same day: the M5 A/B criterion is **not met**, `editRetryHint` stays in, opt-in and off by default, documented as unproven. The doctor A/B could not be run as written because no model on the reference machine is both weak enough to fail check 3 and usable at all (`qwen3.5-9b` through Ollama at 65536 scored 10/10 with check 3 full on the first bare run; LM Studio 400s every tools request on that family, `qwen3-8b` maxes under the harness floor, `ternary-bonsai-27b` will not start at 65536, the two larger ones do not fit 10 GB). A targeted probe through the real ingress replaced it: 10 trials per arm on a conversation whose edit had already been rejected. Mechanism confirmed (hint fired 10/10 with the quirk, 0/10 without). Efficacy not confirmed: the flail it targets happened 0/10 in BOTH arms, and the hint looked directionally worse (byte-exact retry 4/10 bare against 1/10 hint, prose instead of a tool call 0/10 against 3/10) without significance at N=10 (Fisher exact p = 0.30 and p = 0.21) | An adapter that cannot be shown to help must not be sold as one, and the honest failure here is not the quirk, it is the subject: a model that never gets an edit rejected gives the hint nothing to fire on, so the measurement had no headroom to find. Recording that is worth more than a table of twenty tens. Keeping the mechanism is not the same as claiming it works: it is tested, it is off unless asked for, its firing is now counted in the report and in `--json` (an intervention nobody can see cannot be judged), and the doc says in three places that nobody has shown it earns its tokens. The `noCall` signal is the actionable part: 3/10 prose answers instead of tool calls suggests the wording pushes the model out of tool calling, which is a wording problem to test, not a reason to delete the seam the rest of M5 needs | Reverting the quirk (the mechanism is a prerequisite for the retry-with-feedback adapter and the double score, and deleting a tested seam because its first subject was the wrong one throws away the work and the finding); running the 10+10 doctor A/B anyway (2.3 hours of GPU to compare twenty tens); pulling a weaker model to force a failure (a 3B that fails everything never reaches an edit either, so it would measure a different nothing); calling the microbenchmark the M5 criterion (it is one turn on one model, not the reference task); tuning the hint text until the numbers improve on a model that does not have the defect (fitting noise) |
| 47 | 2026-08-09 | Agent routes (SPEC-PROVIDERS §4decies): per-subagent model/provider control through **id algebra only**. A global `agents` table maps `<name>` to a slot-shaped target; the id `claude-lupin-agent:<name>` (typed into an agent's frontmatter `model:`, the Agent tool `model` parameter, or `CLAUDE_CODE_SUBAGENT_MODEL`) resolves through it, bypasses content routes, and logs `agentRoute`. The conventional name `subagents` makes `lupin run` fill `CLAUDE_CODE_SUBAGENT_MODEL` when the user has not set it. Unknown names serve on the normal path with `agentRoute: "unknown:<name>"`. Editable from CLI (`lupin agents`), control API (`POST /v1/lupin/agents`, whole table, atomic) and the TUI (`a`). Ids NOT published in `/v1/models` | Requested twice in the 0.2.0 launch thread and parked since ADR-25. The wire leaves one honest channel: no header or metadata identifies a subagent (anthropics/claude-code#12430 closed "not planned"), while the model id travels verbatim and the client accepts arbitrary full ids in every subagent model knob (docs v2.1.211+, verified 2026-08-09). Id algebra is the ADR-37 pattern, needs zero content inspection (ADR-12 untouched), and the table stays opt-in (ADR-7/ADR-25: nothing routes silently). Atomic whole-table writes are the ADR-34 argument. The client-side precedence (`CLAUDE_CODE_SUBAGENT_MODEL` overrides frontmatter, so blanket and per-agent routing do not compose) is documented wherever the feature is offered | Sniffing the system prompt to detect subagents (version-fragile, content inspection, and #12430 shows the client will not help); a per-profile `agents` table (the routing describes the harness, not a provider: one table, or every profile duplicates it); publishing agent rows in the picker (inert rows with no gesture behind them); a new target shape with a slot field (the slot-target shape already expresses both cases; an agent id names no tier, so delegation lands on sonnet and the user aims that profile's slots); auto-setting the env var without the declared route (ADR-7) |
| 48 | 2026-08-09 | `lupin agents set/unset --wire` (SPEC-CLI §1): on the explicit flag, Lupin writes the agent definition's frontmatter `model:` field (`claude-lupin-agent:<name>` on set, `inherit` on unset). The ONE sanctioned write into the user's harness, bounded: explicit flag per invocation, that single field, old value printed next to the new, every other byte preserved, a file without a frontmatter block refused, built-in agents (no file) named as out of reach. The route in the config is written FIRST and survives a wire failure, which exits 1 with the line to paste by hand | User order 2026-08-09: the ideal UX is "set once from the CLI or the TUI, then done", and the one residual manual step was pasting one line into the frontmatter. ADR-11's spirit (zero side effects on the user's environment) stays the default; this is a scoped exception the user asked for, visible in its own diff line on screen, and reversible with the same command | Editing silently whenever a route is set (ADR-11's spirit, and a surprise edit to a user-authored file); restoring the pre-wire value on unset (Lupin does not record it, and pretending to remember would invent data: `inherit` is the documented client default and is stated as such); fuzzy-matching agent files beyond frontmatter `name:` then filename (a wrong match edits the wrong file); writing into `~/.claude/settings.json` (still forbidden, ADR-11) |
| 49 | 2026-08-09, amended 2026-08-10 | `lupin update` (SPEC-CLI §1): explicit self-update. One registry check (`registry.npmjs.org/lupin-code/latest`) only when invoked, `npm i -g lupin-code@latest` with inherited stdio, and, when a sidecar is on the PATH and cargo exists, a rebuild of the TUI from the sources now shipped in the tarball (`tui/`), built under a temp `CARGO_TARGET_DIR` and copied over the PATH binary only on success. Before npm renames the global package, the updater takes the daemon lifecycle lock, verifies that the recorded live PIDs run the exact daemon and watchdog entrypoints, stops watchdog then daemon, waits for both to exit, and keeps the lock through npm and recovery. It restarts the daemon after a successful update if it was previously running. Detached lifecycle processes use the Lupin state directory rather than the package directory as their working directory. The decision of which steps apply is a pure tested function; the executor only runs it | User request 2026-08-09, including the follow-up "can the TUI rebuild itself with the update": before this the sidecar silently lagged behind every npm update, and the README's manual `cargo build` was the only path. The 0.2.3 to 0.2.4 update then exposed the missing lifecycle boundary on Windows: npm failed with `EBUSY` because the resident daemon or watchdog still anchored `node_modules/lupin-code` through its working directory or loaded native keyring binary. Closing the TUI did not help because both helpers are detached by design. Stopping only the daemon is insufficient because the watchdog immediately takes its port and remains resident. A PID file alone is not ownership because a stale PID can be reused; exact entrypoint verification prevents Lupin from terminating that unrelated process. The shared lock closes the gap where a concurrent `lupin run` could respawn inside npm's replacement window. Restarting preserves the prior running state. Shipping the sources costs little tarball weight and keeps ADR-27 intact: nothing native enters the Node runtime, the build happens only for users who already built one and only at update time. The check is not telemetry: it fires exclusively on the user's own command (§4.3) | An automatic update check on startup (phone-home, against the no-telemetry rule); retrying npm while the locking processes remain (cannot release a Windows directory lock); trusting a numeric PID without checking its entrypoint (PID reuse can kill an unrelated process); killing every Node process (unbounded and unsafe); stopping only the daemon (the watchdog remains resident); an update-only lock (a concurrent daemon start would ignore it); publishing prebuilt sidecar binaries (per-platform artifacts to sign and host, against the sidecar-out-of-npm choice of ADR-27); building inside `node_modules` (a 200MB target dir in the global tree, wiped on every update); rebuilding when no sidecar is on the PATH (installing a surface the user never chose) |
| 50 | 2026-08-10 | Provider onboarding is TUI-native through the existing authenticated control API. On a cold start, bare `lupin` may run an empty in-memory bootstrap daemon; the first persisted profile must retain that daemon's port and local token. The TUI drives provider catalogue, key verification, OAuth polling and risk confirmation directly and never spawns `lupin init` or `lupin login` child commands | The hub is already the user's primary interactive surface, and the control API already owns authenticated state changes. Reusing its verification and persistence functions keeps one source of truth while allowing a user with no config to remain in the TUI from provider choice through the first dashboard | A web UI (new local surface and security boundary); duplicated setup logic in Rust (verification and storage would drift); forcing cold-start users back to the CLI (breaks the hub flow at its most important moment) |
| 51 | 2026-08-12 | Setup is TUI-only: the CLI verbs `init`, `login` and `logout` are removed once the control plane reaches parity (local-runtime discovery and setup, economy preset, save-anyway, official-CLI credential import, OAuth account label, failover offer). Partially supersedes ADR-50, which kept local discovery and multi-account login as explicit CLI paths | One onboarding surface means one place where its promises are tested and kept: the two wizards had already diverged (the economy preset and the route offers existed only in the CLI, the control plane could not create a local profile at all). User decision, stated plainly: everything through the TUI | Keeping two setup surfaces in sync forever (the drift was already measurable); removing the CLI verbs without parity (would orphan the local runtimes and every offer the wizard made) |
